<template>
    <lightning-card>
        <div class="slds-form-element">
            <label class="slds-form-element__label">Select Account</label>
            <div class="slds-form-element__control">
    
                <div class="slds-select_container">
                    <select class="slds-select" onchange={handleAccountChnage}>
                        <option value="">Please select</option>
                        <template if:true={accountDetails} for:each={accountDetails} for:item="acc" for:index="index">
                            <option key={acc.id} value={acc.Id}> {acc.Name}</option>
                        </template>
                    </select>
                </div>
            </div>
    
            <label class="slds-form-element__label">Select Contact</label>
            <div class="slds-form-element__control">
    
                <div class="slds-select_container">
                    <select class="slds-select">
                        <option value="">Please select</option>
                        <template if:true={relatedOpportunity} for:each={relatedOpportunity} for:item="acc"
                            for:index="index">
                            <option key={acc.id} value={acc.Id}> {acc.Name}</option>
                        </template>
                    </select>
                </div>
            </div>
    
            <label class="slds-form-element__label">Select Opportunity</label>
            <div class="slds-form-element__control">
    
                <div class="slds-select_container">
                    <select class="slds-select">
                        <option value="">Please select</option>
                        <template if:true={relatedContact} for:each={relatedContact} for:item="acc" for:index="index">
                            <option key={acc.id} value={acc.Id}> {acc.Name}</option>
                        </template>
                    </select>
                </div>
            </div>
    
            <lightning-card variant="Narrow" title="Hello" icon-name="standard:contact">
                <!-- <lightning-button-icon  icon-name="standard:Contact" variant="border-filled" alternative-text="Show More" slot="actions"></lightning-button-icon> -->
                <div>
                    <lightning-input type="text" variant="standard" name="name" label="Last Name"
                        value="Kripa"></lightning-input>
                    <lightning-button variant="base" label="Submit" title="Submit" onclick={handleClick}></lightning-button>
                </div>
    
            </lightning-card>
        </div>
    </lightning-card>
    

    <!-- <div class="slds-form-element">
            <label class="slds-form-element__label">Select Label</label>
            <div class="slds-form-element__control">
                <div class="slds-select_container">
                    <select class="slds-select">
                        <option value="">Please select</option>
                        <template for:each={accountDetails} for:item="acc" for:index="index">
                            <option key={acc.Id} value={acc.Id}>{acc.Name}</option>
                        </template>
                    </select>
                </div>
            </div>
        </div> -->



</template>
